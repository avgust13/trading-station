services:
  trading-station:
    build: .
    ports:
      - "8888:8888"
    volumes:
      # Blotter journal (data/blotter.json) survives container rebuilds.
      - ./data:/app/data
    restart: unless-stopped
